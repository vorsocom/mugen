[Unit]
Description=Timer for muGen IPC service.

[Timer]
OnBootSec=60                ## Run 60 seconds after (re)boot.
OnCalendar=*-*-* *:00:00    ## Run every hour, on the hour.

[Install]
WantedBy=multi-user.target